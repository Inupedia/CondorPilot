name: Cleanup merged PR 10 branch

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Delete merged branch
        run: |
          if git ls-remote --exit-code --heads origin feat/evidence-report-hardening; then
            git push origin --delete feat/evidence-report-hardening
          fi
